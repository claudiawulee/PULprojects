<%= bootstrap_form_for(@pet.activities, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-8") do |a|%>
    <% pet.activities.each do |act|%>
        <%= a.text_area :action %>
    <%end%>
<%end%>
